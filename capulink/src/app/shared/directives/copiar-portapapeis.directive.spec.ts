import { CopiarPortapapeisDirective } from './copiar-portapapeis.directive';

describe('CopiarPortapapeisDirective', () => {
  it('should create an instance', () => {
    const directive = new CopiarPortapapeisDirective();
    expect(directive).toBeTruthy();
  });
});
