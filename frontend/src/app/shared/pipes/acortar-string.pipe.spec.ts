import { AcortarStringPipe } from './acortar-string.pipe';

describe('AcortarStringPipe', () => {
  it('create an instance', () => {
    const pipe = new AcortarStringPipe();
    expect(pipe).toBeTruthy();
  });
});
